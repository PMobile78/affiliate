/*export */
class AffiliateTags {
    constructor (affiliate_id, tag_id, created_date) {
        this.affiliate_id = affiliate_id
        this.tag_id = tag_id
        this.created_date = created_date
    }
}

module.exports = AffiliateTags

